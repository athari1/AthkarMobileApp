<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ites.makkahleave.athkar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Mokadema"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppTheme.NoActionBar" />
        <activity
            android:name=".Athkar_Sabeh"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppTheme.NoActionBar" />
        <activity
            android:name=".Athkar_Mase2"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppTheme.NoActionBar" />
        <activity android:name=".Thekr_detailsActivity"></activity>
    </application>

</manifest>
